# react_real_time

For setup read Readme.md inside each follow projects

1./ Server project ( Websoket server)
        /server ( nodejs )

2./ Client project ( Websoket client)
        /vite-project ( React + vite )